@model IPagedList<DoctorModel.Doctor>

@using PagedList.Mvc;
@using PagedList;

@* Part 59*@
@{
    ViewBag.Title = "Doctor List Page";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.First().Name)*@
            @Html.ActionLink("Name", "Index", new { sortBy = ViewBag.NameSort })
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.First().Gender)*@
            @Html.ActionLink("Gender", "Index", new { sortBy = ViewBag.GenderSort })

        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().WorkEx)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Fee)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().EmailId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Website)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().tblSpeciality.Speciality)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkEx)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Website)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tblSpeciality.Speciality)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }
    @*@Html.PagedListPager(Model, page => Url.Action("Index", new { page }), new PagedListRenderOptions() { Display = PagedListDisplayMode.IfNeeded, DisplayPageCountAndCurrentLocation = true })*@
    @Html.PagedListPager(Model, page => Url.Action("Index", new { page }), new PagedListRenderOptions() { Display = PagedListDisplayMode.IfNeeded, DisplayItemSliceAndTotal = true })
</table>

Doctor List retrieved @@ @DateTime.Now.ToString()
